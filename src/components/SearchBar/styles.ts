import { StyleSheet } from "react-native";

export default StyleSheet.create({
    container: {
        padding: 8,
        flexDirection: "row",
        backgroundColor: "#eee",
        borderRadius: 20,
        alignItems: "center"
    },
    input: {
        fontSize: 20,
        marginLeft: 10,
        width: "90%"
    },
    searchIcon: {
        marginLeft: -5
    }
});
