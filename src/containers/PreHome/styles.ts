import { StyleSheet } from "react-native";

export default StyleSheet.create({
    container: {
        marginTop: "25%",
        marginHorizontal: "8%"
    },
    cardTitle: {
        fontWeight: "bold",
        fontSize: 18,
        marginBottom: 10
    },
    cardButton: {
        marginTop: "10%",
        paddingHorizontal: 10,
        borderRadius: 10
    }
});
