import { StyleSheet } from "react-native";

export default StyleSheet.create({
    container: {
        paddingHorizontal: "10%",
        marginTop: "20%"
    },
    searchBar: {
        marginBottom: "10%"
    }
});