import { StyleSheet } from "react-native";

export default StyleSheet.create({
    tabBar: { 
        position: "absolute",
        marginHorizontal: "10%",
        marginBottom: "7%",
        borderRadius: 20
    }
});