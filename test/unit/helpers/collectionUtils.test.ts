// import UsersCollection from "../../../src/services/firebase/db/users"
// import { parseCollection } from "../../../src/helpers/collectionUtils";

// describe("users should be parsed correctly", () => {
//     it("user find returns a type compatible with IUser", async () => {
//         const userFromDatabase = await UsersCollection.find(1);
//         expect(typeof parseCollection(userFromDatabase)).resolves.toBe("IUser");
//     });
// });

test("placeholder", () => {
    expect(1).toBe(1);
});